# 各縣市景點

此專案主要是透過 redux、hooks、IntersectionObserver 方法來完成

## 切換全部景點和各縣市景點

透過左邊的側邊選單來切換不同的縣市景點

## 專案架構

- src
  - /actions `存放 action`
  - /reducers `存放 reducer`
  - /store `設置 store`
  - /components `共用組件 側邊選單`
  - /hooks `自定義的 hook`
  - /pages `主要要面有 全部景點和各縣市景點`
  - App.js ``
  - index.js
